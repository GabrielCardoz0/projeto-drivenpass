export type User = {
    email: string,
    password: string
  };

  export type credential = {
    title: string,
    url: string,
    username: string,
    password: string,
    userId: number
  };

  export type network = {
  title: string,
  network: string,
  password: string,
  userId: number
  };